apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xwave.fullname" . }}-test-connection"
  labels:
    {{- include "xwave.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xwave.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
